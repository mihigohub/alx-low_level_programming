#include "main.h"
/**
 * print_alphabet - function to print alphabets in lowercase
 * Return: void
 */
void print_alphabet(void)
{
	char A;

	for (A = 'a'; A <= 'z'; A++)
		_putchar(A);
	_putchar(10);
}
