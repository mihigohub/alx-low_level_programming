#include "main.h"
/**
 * add - function that adds two integers and
 * returns the result.
 * @a: first integer
 * @b: second integer
 * Return: returns the sum of a and b
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
