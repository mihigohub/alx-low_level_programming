#include <stdio.h>
#include "main.h"
/**
 * main - prints all the arguments that will be passed
 * @argc: the number of arguments
 * @argv: the array of arguments
 * Return: always zero
 */
int main(int argc, char *argv[])
{
	int a;

	for (a = 0; a < argc; i++)
	{
		printf("%s\n", argv[a]);
	}
	return (0);
}
